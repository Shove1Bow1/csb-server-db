const { ProvidersSchema } = require('../entities/providers.entity');
const providers = ProvidersSchema;
function getProvider(req, res, next) {
    // try {
    //     const provider = providers.findOne({
    //         where: {
    //             region: {
    //                 name
    //             }
    //         }
    //     })
    // }
    // catch(error){
    //     res.status('400').send(error);
    // }
}